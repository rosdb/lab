{
  "name": "natours",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:pug": "pug src --out dist --pretty",
    "build:scss": "node-sass src/styles/main.scss dist/style.css",
    "build:all": "run-p -cln build:*",
    "watch:pug": "npm run build:pug -- --watch",
    "watch:scss": "npm run build:scss -- --watch --recursive",
    "watch:all": "run-p -cln watch:*"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "node-sass": "^4.12.0",
    "npm-run-all": "^4.1.5",
    "pug": "^2.0.3"
  }
}
